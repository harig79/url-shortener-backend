# URL Shortener Backend

A simple and scalable URL shortener backend built using **Node.js**, **Express**, and **MongoDB**.  
This API allows users to generate short URLs for long links and stores them persistently in the database.

---

## 🚀 Features

- Generate short URLs using `nanoid`
-
